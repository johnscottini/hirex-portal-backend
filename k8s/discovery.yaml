apiVersion: apps/v1
kind: Deployment
metadata:
  name: discovery
  namespace: hirex
spec:
  replicas: 1
  selector:
    matchLabels:
      app: discovery
  template:
    metadata:
      labels:
        app: discovery
    spec:
      containers:
        - name: discovery
          image: jonathanscottini/discovery:1.0
          env:
            - name: SERVER_PORT
              value: "8761"
            - name: SPRING_APPLICATION_NAME
              value: "discovery"
            - name: EUREKA_CLIENT_REGISTER_WITH_EUREKA
              value: "false"
            - name: EUREKA_CLIENT_FETCH_REGISTRY
              value: "false"
          ports:
            - containerPort: 8761

---
apiVersion: v1
kind: Service
metadata:
  name: discovery
  namespace: hirex
spec:
  selector:
    app: discovery
  ports:
    - port: 8761
      targetPort: 8761
